description: descriptor for mgmt_infra
properties:
  deployment_location:
    required: true
  jumphost_internal_ip:
    read-only: true
  jumphost_public_ip:
    read-only: true
  jumphost_username:
    default: ubuntu
  jumphost_password:
    default: ubuntu
  mgmt_network_id:
    read-only: true
  mgmt_security_group_id:
    read-only: true
composition:
  infra:
    properties:
      jumphost_internal_ip:
        read-only: true
      jumphost_public_ip:
        read-only: true
      jumphost_username:
        default: ubuntu
      jumphost_password:
        default: ubuntu
      mgmt_network_id:
        read-only: true
      mgmt_security_group_id:
        read-only: true
    type: $lmctl:/contains:/infra:/descriptor_name
designerLayout:
  description: >-
    The following information is solely used to determine the presentation of
    this Assembly Design in the LM UI. It does not form part of
    operational/behavioural description of the Assembly and should not be edited
    directly.
  elements:
    infra:
      x: 838
      y: -30.666671752929688
    assembly::mgmt_infra::1.0:
      x: 0
      y: 180
